- host: grupo1
  remote_user: root
  
  # vars:
  #  una_variable: valordevar1
  
  tasks:
    - name: "Instalar PostgreSQL"
      
      yum:
        name: postgresql
      register: salidayum
    
    - debug: var=salidayum
   
