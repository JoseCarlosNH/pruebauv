- hosts: grupo
  remote_user: jnunezh
  
  # vars:
  #  una_variable: valordevar1
  
  tasks:
    - name: "Instalar PostgreSQL"
      
      apt:
        name: postgresql
      register: salidayum
    
    - debug: var=salidayum
   
